{
  "name": "allecto-platform",
  "private": true,
  "packageManager": "pnpm@10.18.2",
  "workspaces": [
    "apps/*",
    "backend/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "@types/bcryptjs": "^3.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "convex:dev": "pnpm -F backend-convex dev",
    "convex:deploy": "pnpm -F backend-convex deploy"
  }
}
